version: '3.3'

services:
  FundosInvestimento.API:
    image: fundosinvestimento
    container_name: fundosinvestimento
    restart: unless-stopped
    build:
      context: .
      dockerfile: FundosInvestimento.API/Dockerfile
    ports:
        - '8091:80'
    volumes:
        - './upload:/var/temp/FundosInvestimento'
        - './temporario:/var/temp/'